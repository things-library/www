{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://thingslibrary.io/schemas/1.0.0/itemtype.json",
    "$comment": "Item types are used to explain all that is possible attribute and attachment wise with a thing.",
    "$version": "1.0.0",

    "type": "object",
    "properties": {
        "key": {
            "type": "string",
            "title": "Key",
            "minLength": 2,
            "pattern": "^[a-z0-9_]*$"
        },

        "name": {
            "type": "string",
            "title": "Name",
            "minLength": 2,
            "maxLength": 50
        },

        "description": {
            "type": "string",
            "title": "Description"
        },

        "locked": {
            "type": "boolean",
            "title": "Locked",
            "description": "Prevent attachments from outside of the attachment listing",
            "default": false
        },

        "system": {
            "type": "boolean",
            "title": "System",
            "description": "Indicates that this item type is managed by the system and can't be edited by a user",
            "default": false
        },

        "attributes": {
            "type": "array",
            "title": "Attributes",
            "items": {
                "$ref": "https://thingslibrary.io/schemas/1.0.0/attribute.json"
            }
        }
    },
    "required": [ "key", "name" ]
}
